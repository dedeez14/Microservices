const { warehouseSchemas } = require('./warehouse')
const { locationSchemas } = require('./location')
const { inventorySchemas } = require('./inventory')
const { transferSchemas } = require('./transfer')

module.exports = {
  warehouseSchemas,
  locationSchemas,
  inventorySchemas,
  transferSchemas
}
